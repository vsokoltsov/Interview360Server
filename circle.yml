machine:
  python:
    version: 3.5.2
dependencies:
  pre:
    - pip install -r requirements.txt
database:
  override:
    - postgres -c 'psql -c "CREATE USER vagrant WITH PASSWORD '"'"'vagrant'"'"' SUPERUSER;"

test:
  override:
    - python app/manage.py test
