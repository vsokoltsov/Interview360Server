machine:
  python:
    version: 3.5.2
dependencies:
  pre:
    - pip install -r requirements.txt
database:
  override:
    - createdb interview_manager
    - echo "CREATE USER vagrant WITH PASSWORD 'vagrant' SUPERUSER;" | psql -U postgres

test:
  override:
    - python ./interview_manager/app/manage.py test
